## そもそも部分列とは
部分列(Subsequence): 元の文字の順序を保ったまま、いくつかの文字を間引いて取り出したもの
部分文字列(Substring): 元の文字列の中で、連続して並んでいる一部を切り出したもの

## リングバッファは現実世界のインフラを支えている
リングバッファは競プロの小道具ではなく、コンピュータの通信のほぼ全てに使われている。ネットワークカード(NIC)がバケットを受信すると、
カーネルとNICが共有するリングバッファ(RX ring)に書き込まれ、カーネルがheadを進めながら処理する。
キーボード入力、シリアル通信、オーディオ再生のバッファもリング。なぜ連結リストではなくリングかというと、メモリを事前確保するためにmallocが不要。CPUキャッシュに乗りやすい。head, tailの更新で行ける。ロックなしで平衡動作できる。
高頻度取引システムやゲームエンジンのスレッド間通信も lock-freeリングバッファが定番
容量は2の冪乗にして、%capacityを & (capacity-1)　に置き換える。
「満杯」と「空」をどちらもhead == tailになる

## キュー2本でスタックを作れ。


## 「何ができるか(インターフェース)」と「どう作るか(実装)」を分けて考える。という計算機科学で最も重要な設計原理の一つ。「キュー」とは本来enqueueとdequeueという操作の約束事だけを指す抽象データ型(ADT: Abstruct Data Type)で、配列で作るかリストで作るかヒープで作るかは、実装の自由。　
# 性能要件に応じて、素朴配列 => リングバッファ => lock-free と実装方法を変えることができる
# 
#
# 学習中
#
# デッドコード(dead code)と到達不能コード - コンパイラはどこまで気づけるか。
#
# 構文的には到達可能だが、意味的には絶対に実行されない。
#
# return の直後に書かれた文のような構文的到達不能コードは、コンパイラが制御フローグラフを見るだけで機械的に検出できます。
#
# 一方、今回のような意味的デットコードー　「wordListSizeが0」ならreturn済みである。
# という関数間の論理を追わないと分からないものは、一般的には停止性問題に帰着するため完全な自動検出が不可能で、コンパイラは警告を出せない。
# 人間のコードレビューや縁深い静的解析の両分。
#
# コンパイラは、制御フローグラフを見るだけで機械的に検出できます。
# gccの -Wunreachable-codeなど
#
# LeakSanitizerと「所有権」
# 先ほどASan付きでテストを走らせたら、LeakSanitizerが、「124 bytes leaked in 9 allocations」と、malocした行番号付きで全リークを列挙しました。
# リークの根本原因は技術ではなく所有権(ownership)の未定義です。このNodeをfreeする責任は誰にあるのか、
# enqueueした人か、dequeueした人か、ladderLengthか、それともきまていないから、どこにもfreeが書かれていない。
# Cでは古典的パターンがある。
# (1)スコープ所有：確保した関数が末尾で必ず解放する（main.c方式）
# アリーナ方式：個々のNodeを追わず、大きな塊から切り出して、最後に塊ごと解放。
# 所有権の移転を明文化。Rustは型システムで3を矯正した言語。
#
#
