ヒープ
完全二分木 + Max or Min

左右のノードに大小の違いはない。

      1
     / \
    3   2
   / \  
  7   5  

こんなのでもいい。
親 <= 子

の条件が守れられている完全二分木は、Min-Heap

Min-Heapの役割は、「最小値を素早く取り出すこと」だけが目的


- 最小値は常にrootにある。=> O(1)で取り出せる。
- 左右の順番は問はない => 挿入がシンプルになる。


今回のk番目に大きい値を求めるのにどういう風にデータ構造・アルゴリズムを使うかというと、

k番目に大きな値をrootとした、Min-Heapを実装する。


